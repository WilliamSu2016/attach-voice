# attach-voice — 运行时与开发依赖（版本固定，见 AGENTS.md 实现规则 6）

# GUI（含 QtMultimedia，用于分段试听）
PyQt6==6.9.1

# TTS：仅允许 src/core/edge_tts_provider.py 直接 import（规则 B10）
edge-tts==7.2.8

# 精确读取合成音频（mp3）实际时长，用于 synthesize() 返回值校验（PRODUCT.md#R5）
# 纯 Python 实现，不依赖 ffmpeg/ffprobe（规则 B6 仅 video_processor.py 可调用 ffmpeg/ffprobe）
mutagen==1.48.1

# 测试
pytest==8.4.2
pytest-cov==6.3.0
