# plan.md Review 报告

## 结论
方向正确、技术选型合理（Python + PyQt6 + edge-tts + ffmpeg 对"免费 + 桌面 + 脚本配音"这个组合是当前最优解）。
但有 **4 个高风险问题** 和 **6 个中等问题**，若不在计划阶段解决，会在实现中返工。

---

## 高风险问题（必须修正）

### H1. 音视频时长不匹配的策略含糊不清 —— 最大设计风险
原文："MVP 阶段可简单地以配音时长为准（视频画面循环/定格或裁剪）"

问题：三个方案（循环 / 定格 / 裁剪）互相矛盾且都会破坏原视频，用户预期通常是**视频画面绝对不动**。
一句"或"把最核心的产品决策推给了实现阶段。

**建议**：默认策略应为 **画面永不改动**，音频适配画面：
- 配音短于视频 → 音频尾部补静音（ffmpeg `apad`）
- 配音长于视频 → 提供两个明确选项：
  1. `裁剪音频`（截断超出部分，默认）
  2. `延长视频`（末帧定格 `tpad`，保证配音完整）
- UI 上必须显示"视频时长 X 秒 / 配音时长 Y 秒"的对比提示，让用户在导出前知情

### H2. 单段整篇配音在真实场景中不够用
原计划只有一个大文本框 → 一整段音频 → 从 0 秒开始贴。
实际给视频配音时，旁白几乎总是分成多段、对应画面的不同时间点。若 MVP 不支持，产品基本不可用，且事后改造涉及数据模型重构（单段 → 段列表），返工成本高。

**建议**：MVP 就引入**最小化的分段模型**（一个 `Segment` 列表：文本 + 起始时间），
UI 用一个可增删行的表格。这是"多花 20% 工作量、可用性翻倍"的取舍。
`script_parser.py` 从"二期可选"提升为 MVP 组成部分（先支持简单的 `[00:15] 文本` 格式和 .srt 导入）。

### H3. 未指定 ffmpeg 编解码策略 —— 直接影响性能
计划完全没提视频流是否重编码。若默认重编码，10 分钟视频要跑几分钟；
用 `-c:v copy` 只重编码音轨则是秒级完成。这是必须在计划里钉死的技术决策。

**建议**：明确 `-c:v copy -c:a aac`，仅在容器不兼容时才回退到重编码。

### H4. edge-tts 是非官方逆向接口，存在单点失败风险
edge-tts 走的是微软 Edge 朗读功能的私有接口，**非官方公开 API**，历史上出现过接口变更导致库失效、以及 IP 限流。
计划把 Piper 列为"二期"，等于把整个应用的可用性押在一个不受控的第三方接口上。

**建议**：MVP 阶段不实现 Piper，但**必须先定义 `TTSProvider` 抽象基类**（`list_voices()` / `synthesize()`），
edge-tts 作为其第一个实现。这样后续接入 Piper / 其他引擎是加一个文件，而不是改遍全项目。

---

## 中等问题

### M1. 试听功能缺少实现方案与依赖
PyQt6 播放音频需要 `QtMultimedia`（`PyQt6` 的 multimedia 模块），在部分环境需单独安装且 PyInstaller 打包易漏。
→ 需在 requirements 与打包步骤中显式列出并验证。

### M2. ffmpeg 分发方式与许可未决
"提示用户安装"体验差；"随程序打包"则涉及 ffmpeg 的 LGPL/GPL 许可（含 libx264 的构建为 GPL）。
→ 建议：优先查找系统 ffmpeg，找不到时引导用户一键下载 LGPL 版静态构建到应用目录（而非直接内嵌分发），并在 README 注明许可。

### M3. 长文本未考虑分块
edge-tts 对超长单次请求可能超时或截断。
→ 需按句子/长度分块合成再拼接（分段模型 H2 天然缓解了这一点）。

### M4. 缺少任务取消能力
TTS 生成和 ffmpeg 导出都是耗时任务，QThread 里必须有取消机制（终止 ffmpeg 子进程），否则用户只能强杀进程。

### M5. 缺少验收标准
第 8 步"验证音画同步效果"不可度量。
→ 应写成可验证的断言：输出视频时长与预期一致（误差 < 0.1s）、分段起始时间偏差 < 0.2s、输出文件可被主流播放器打开。

### M6. 缺少配置持久化
音色、语速、输出目录等每次重开都要重选。用 `QSettings` 几行代码即可解决，属高性价比项。

---

## 其他小建议
- `src/` 布局需明确以包方式运行（`python -m src.main`），避免相对导入在 PyInstaller 下出错
- 建议增加"仅导出音频"功能（导出配音 mp3），便于用户拿去别处使用，成本几乎为零
- 音色列表应支持按语言过滤（edge-tts 有 400+ 音色，不过滤无法使用）
- 叠加模式下原声降低音量的比例应可调（默认 20%），而非写死
